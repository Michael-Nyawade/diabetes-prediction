data:
  raw: data/raw
  processed: data/processed
  raw_filename: diabetes.csv
  invalid_zero_columns:
    - Glucose
    - BloodPressure
    - SkinThickness
    - Insulin
    - BMI
  target_column: Outcome

split:
  test_size: 0.2
  random_state: 42
  stratify: true

model:
  random_state: 42
  max_iter: 1000
